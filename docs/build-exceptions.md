# Build Exceptions

This file records build diagnostics that the user explicitly approved leaving unresolved.

## Rules

- Add an entry only after explicit user approval.
- Include the exact command or build context where the diagnostic appears.
- Summarize the diagnostic precisely enough that future agents can recognize it.
- Remove the entry as soon as the underlying issue is fixed.

## Active Exceptions

None.
